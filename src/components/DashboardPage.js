import React from 'react';

const DashboardPage = () => (// there is no need for {return();} we just can write the code directly and without return using shorthand syntax
    <div>
      Dashboard page content
    </div>
);

export default DashboardPage;